[pytest]
# pytest 配置
# 注意：-p no:logging 禁用 pytest 的日志捕获，避免与项目 logger_config 冲突
addopts = -p no:logging --no-header -v
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*