# ChainSight 性能评估与架构升级方案 (单机版 vs 集群版)

## 1. 性能现状评估

### 1.1 当前架构特点 (单机优化版)
*   **计算引擎**: 基于 Python (Pandas/Numpy/Scipy) + DuckDB (向量化执行)。
*   **存储层**: PostgreSQL (持久化) + Parquet/Excel (中间状态/配置)。
*   **并行度**: 
    -   `ParallelExecutor` 提供同天内不同模块 (M1/M4/M5) 的线程级并行。
    -   DuckDB 内部多线程并行执行 SQL。
*   **局限性**:
    -   **内存受限**: Pandas 操作依赖单机 RAM，处理亿级数据时易触发 OOM。
    -   **计算瓶颈**: 日循环 (Main Loop) 为串行结构，仿真周期长时时间累积严重。
    -   **存储瓶颈**: PostgreSQL 在超大规模扫描 (Analytical Queries) 性能不如专用 OLAP。

### 1.2 单机版性能天花板
| 维度 | 预估上限 | 瓶颈点 |
| :--- | :--- | :--- |
| 物料x地点 (Entities) | 10万 - 50万 | 内存占用 & 映射查找耗时 |
| 仿真天数 (Days) | 365天 | 串行循环累积耗时 |
| 日均订单量 (Daily Orders) | 10万级 | `merge` 与 `sort` 操作性能下降 |
| 总数据量 (Total Rows) | ~1亿 (DuckDB 内存限制下) | 磁盘 I/O 与 索引开销 |

---

## 2. 单机版 vs 集群版性能差异

| 特性 | 单机版 (Current) | 集群版 (Proposed) | 性能差异原因 |
| :--- | :--- | :--- | :--- |
| **计算模式** | 垂直扩展 (Scale-up) | 水平扩展 (Scale-out) | 集群可利用成百上千个核心并行处理不同场景/分片。 |
| **数据读取** | 本地 I/O (SSD) | 分布式 I/O (S3/HDFS/Ceph) | 集群拥有更高的总聚合带宽，支持并行加载。 |
| **大规模关联** | 内存关联 (Hash Join) | 分布式 Shuffle/Broadcast | 集群突破单机内存限制，支持超大规模数据关联。 |
| **仿真并发** | 串行天数运行 | 场景级/实体级分片并行 | 集群可同时运行多个“平行宇宙”仿真（敏感度分析）。 |
| **延迟 (Latency)** | **低** (无网络开销) | **中** (存在网络通信/序列化开销) | 小规模数据下单机版更快。 |
| **吞吐 (Throughput)** | **低** | **极高** | 大规模数据（PB级）集群具备压倒性优势。 |

---

## 3. 数据量级与数据库更换建议

### 3.1 阶段建议
*   **量级 < 5000万行 (总数据量)**: 
    -   **推荐**: 维持现有 **PostgreSQL + DuckDB**。
    -   **原因**: 运维简单，DuckDB 在单机 OLAP 性能上甚至优于多数集群数据库。
*   **量级 5000万 - 5亿行**:
    -   **推荐**: **ClickHouse (单机/集群)** 或 **StarRocks**。
    -   **原因**: 原生列式存储，支持极快的大规模聚合运算，适合替代 DuckDB 处理持久化的大数据集。
*   **量级 > 5亿行 或 复杂分布式ETL**:
    -   ** recommendations**: **Spark / Flink + Iceberg/Delta Lake**。
    -   **原因**: 纯计算引擎，处理海量历史订单清洗与物料分片。

### 3.2 底层储存更换对比方案
| 数据库 | 类型 | 适用场景 | 迁移成本 | 优势 |
| :--- | :--- | :--- | :--- | :--- |
| **PostgreSQL** | RDBMS | 元数据、配置、小规模结果 | - | 成熟、事务支持强 |
| **DuckDB** | Embedded OLAP | 单机高性能分析、Pandas替代 | 低 | 速度极快，无需安装服务端 |
| **ClickHouse** | Distributed OLAP | 海量订单统计、明细查询 | 中 | 极致的压缩比和查询性能 |
| **StarRocks** | Real-time OLAP | 实时库存平衡、多表关联 | 中高 | 兼容 SQL 强，支持复杂 Join |

---

## 4. 架构优化与升级 Plan (对比方案版)

### Phase 1: 系统体检与基准测试 (现阶段)
1.  **性能基准记录**: 使用 `PerformanceProfiler` 对目前 30d/90d/365d 仿真进行全过程耗时打点。
2.  **压力测试**: 生成模拟数据（100k, 1M, 10M 订单量级），观察 `Memory Usage` 曲线。

### Phase 2: 存储层升级 (可选)
*   **方案 A (轻量级升级)**: 保持 PG，引入 `TimescaleDB` 插件提升时间序列（日级库存）处理能力。
*   **方案 B (高性能 OLAP)**: 部署 `ClickHouse` 替换原有的 DuckDB 持久化部分，作为计算后的结果仓库。

### Phase 3: 计算层由“单机分发”转向“分布式分发”
*   **技术选型**: 引入 **Ray** 或 **Celery**。
*   **并行策略**: 
    -   将“物料分组”作为 Task 分发到不同 Node。
    -   Orchestrator 仅保留状态汇聚功能，计算下沉到 Worker 节点。

### Phase 4: 开发/演进路线
1.  **短期**: 优化 `main_integration.py` 循环逻辑，利用 I/O 并行异步加载下一天数据。
2.  **中期**: 数据库由 PG 转向 `PostgreSQL + ClickHouse` 双引擎架构。
3.  **长期**: 将核心算法容器化，部署至 K8s 集群，使用 Spark 处理预热计算。

---

**结论**: 如果当前数据量级在千万级以下，盲目转向集群版会引入不必要的复杂度（网络延迟、运维成本），建议优先压榨 **DuckDB + PostgreSQL** 的单机潜力。一旦数据跨过“内存线”（如 100GB+），则必须考虑 **ClickHouse** 或 **分布式集群架构**。
